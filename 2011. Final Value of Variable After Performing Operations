class Solution {
    public int finalValueAfterOperations(String[] op) {
       int count =0;
        for(String s: op){
            if(s.equals("++X") || s.equals("X++"))
                count++;
            else
                count--;
            
        }
        return count;
    }
}
