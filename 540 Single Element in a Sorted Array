class Solution {
    public int singleNonDuplicate(int[] nums) {
        int i=0;
        int n = nums.length;
       while(i<n-1){
           if(nums[i]!=nums[i+1]){
               return nums[i];
           }
           i=i+2;
       }
        return nums[i];
    }
}
