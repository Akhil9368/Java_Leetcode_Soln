class Solution {
    public int titleToNumber(String str) {
       
        int sum =0;
        int j=0;
        for(int i =str.length()-1;i>=0;i--,j++){
            sum +=  (str.charAt(i)-64)*Math.pow(26,j);
            
        }
        return sum;
    }
}
